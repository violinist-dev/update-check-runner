name: Update
on:
  workflow_dispatch:
  schedule:
    - cron: '2 * * * *' # every hour
  # By using push as well, we make sure our PRs are updated when the main branch has new commits.
  push: 

jobs:
  update:
    uses: violinist-dev/update-action/.github/workflows/update.yml@main

